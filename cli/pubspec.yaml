name: cli
description: A sample command-line application.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.1.1

dependencies:
  args: 2.4.2
  common:
    path: ../common
  path: 1.9.0
  crypto: ^3.0.3          # SHA-256 hashing for code phrases
  uuid: ^4.0.0            # Session IDs
  http: ^1.1.0            # HTTP client for receiver
  basic_utils: ^5.7.0     # Certificate generation
  convert: ^3.1.1         # Hex encoding

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

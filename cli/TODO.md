when renaming fails, the process doesn't die in the sender's side.


no more hmac, made some changes, check if everything still works

we left at changing the problem of having to rename each file individually instead of the correct way, renaming the directory. (When there is conflict.)

